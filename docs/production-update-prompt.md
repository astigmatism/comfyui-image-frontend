# Prompt for the next update

> Run `/host/home/astigmatism/comfyui-image-frontend/update_production` to deploy
> the latest `origin/main` (omit `/host` if already on the native host). Follow
> the printed job-status commands, wait for that same job to finish, and report
> its exit code, deployed SHA and verification result. Do not write deployment
> scripts or configure SSH. The command owns backup, build, restart and checks.
